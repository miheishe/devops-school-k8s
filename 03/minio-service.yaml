apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: minio
  name: minio-app-ingress
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 9001
  selector:
    app: minio
  type: ClusterIP
status:
  loadBalancer: {}
